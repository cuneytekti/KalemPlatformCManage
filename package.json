{
  "name": "kalem-platform-cmanage",
  "version": "1.0.1",
  "private": true,
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "build": "npm run build --workspace api && npm run build --workspace web",
    "test": "npm test --workspace api"
  }
}
